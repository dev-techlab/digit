# dependencies
node_modules

# next.js
.next
out
build

# testing
coverage

# env files — real config/secrets for whatever machine runs the app (dev
# laptop or prod server). .env.example is intentionally NOT ignored — it's
# the committed, secret-free template; copy it to .env and fill in real values.
.env
.env.local
.env.*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editor/OS
.vscode/*
!.vscode/extensions.json
.idea

# vercel
.vercel

# Local-disk media (dev storage driver fallback)
/public/media/
